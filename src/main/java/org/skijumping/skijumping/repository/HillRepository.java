package org.skijumping.skijumping.repository;

import org.skijumping.skijumping.model.Hill;
import org.springframework.data.repository.CrudRepository;

public interface HillRepository extends CrudRepository<Hill,Integer> {
}
