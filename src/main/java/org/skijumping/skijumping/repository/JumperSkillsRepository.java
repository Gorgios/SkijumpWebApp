package org.skijumping.skijumping.repository;

import org.skijumping.skijumping.model.JumperSkills;
import org.springframework.data.repository.CrudRepository;

public interface JumperSkillsRepository extends CrudRepository<JumperSkills,Integer> {
}
